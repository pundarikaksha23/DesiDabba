export function LoadingScreen() {
  return (
    <div className="grid min-h-screen place-items-center bg-cream-50 text-maroon" role="status" aria-live="polite">
      <span className="eyebrow animate-pulse">Preparing Desi Dabba</span>
    </div>
  )
}
